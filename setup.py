from setuptools import setup, find_packages
setup(name='pyshader',
      version='1.0',
      packages=['pyshader'],
      install_requires=['Pillow', 'numpy', 'PyOpenGL', 'OpenEXR', 'pyaudio', 'pyrr']
      )
